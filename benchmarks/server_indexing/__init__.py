"""Real server-backend indexing benchmark (OpenSearch/Elasticsearch).

See ``docs/indexing_benchmarks.md`` (server-indexing section) for how to
run it, and ``benchmarks/indexing/`` for the sibling offline/local-backend
suite this one deliberately does not duplicate or replace.
"""
